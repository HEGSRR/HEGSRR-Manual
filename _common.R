# example R options set globally
# https://stackoverflow.com/a/66753995
options(tidy.opts = list(width.cutoff = 60), tidy = TRUE)

# example chunk options set globally
knitr::opts_chunk$set(
  comment = "#>",
  collapse = TRUE
  )
